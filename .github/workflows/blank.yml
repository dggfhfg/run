# 这是一个基本的工作流程，可以帮助你从 Actions name: CI # 控制工作流程何时运行: # 触发推送或拉取请求事件上的工作流程，但只针对主分支推送: [ main ] pull _ request: branches: [ main ] # 允许你从 Actions 选项卡 workflow _ dispatch 中手动运行这个工作流程: # 工作流程运行是由这个工作流包含一个叫做“ build”build 的作业: # 作业将在运行时运行的运行器类型: ubuntu-latest # Steps 表示作业步骤中将执行的一系列任务: # Checks-out your repository under $GITHUB _ workspace,所以您的作业可以访问它-uses: actions/checkout@v2 # 使用 runners shell-name: Run a one-line script Run: echo Hello，world！使用 runners shell-name: Run a multi-line script Run: | echo Add other actions to build、 echo test 和 deploy your project 运行一组命令。
